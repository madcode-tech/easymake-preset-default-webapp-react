const path = require('path');
const webpack = require('webpack');

module.exports = function(
  rootConfig = [],
  api,
  { testingContexts } = {}
) {
  /*rootConfig.push({
    //some extra plugins
  })*/

  return rootConfig;
};
